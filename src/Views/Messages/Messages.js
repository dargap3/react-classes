import React from 'react';

import Header from '../../Components/Header';

const Messages = () => (
  <div>
    <Header title="Messages" />
    <p>No tienes mensajes en el momento</p>
  </div>
);

export default Messages;